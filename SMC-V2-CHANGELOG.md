# SMC V2 Homepage + Login

This package is based on the uploaded `smc-main.zip` and actually replaces:
- app/page.tsx
- app/login/page.tsx

It also adds the supplied SMC logo as `public/smc-logo.jpg`.
Firebase config and existing modules are preserved.
